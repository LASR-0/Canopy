node_modules/
dist/
dist-build/
out/
build/
*.log
.DS_Store
# TypeScript incremental build cache — regenerated by every build, so leaving it
# tracked causes spurious diffs when moving between machines.
*.tsbuildinfo
# Editor / OS noise
.vscode/
.idea/
Thumbs.db
# local data the backend writes at runtime
*.sqlite
*.sqlite-journal
*.db
*.db-shm
*.db-wal
*.db-journal
data/
/archive/
